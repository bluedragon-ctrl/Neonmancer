{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "strings.schema.json",
  "title": "UI text (data/strings.json)",
  "description": "Every text the game shows, by key. {name} marks a value filled in by the game, e.g. \"v{version}\". Room and biome names live in their own files.",
  "type": "object",
  "required": ["schemaVersion", "strings"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "$ref": "common.schema.json#/$defs/schemaVersion" },
    "strings": {
      "type": "object",
      "description": "Key → text. Keys are dotted groups, e.g. \"hud.integrity\". The listed keys are the ones the game uses; others are allowed while drafting.",
      "propertyNames": { "pattern": "^[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)+$" },
      "additionalProperties": { "type": "string" },
      "required": [
        "game.title",
        "game.version",
        "hud.integrity",
        "hud.energy",
        "hud.movementGrid",
        "hud.movementScreen",
        "hint.fullscreen",
        "banner.room",
        "banner.biome",
        "msg.boot",
        "msg.welcome",
        "msg.die",
        "msg.void",
        "msg.derez",
        "msg.respawn",
        "msg.plug",
        "msg.movementGrid",
        "msg.movementScreen"
      ],
      "properties": {
        "game.title": { "type": "string", "description": "Game name in the HUD corner." },
        "game.version": { "type": "string", "description": "Version next to the name; {version} is the game version." },
        "hud.integrity": { "type": "string", "description": "Label of the integrity (health) bar." },
        "hud.energy": { "type": "string", "description": "Label of the energy (mana) bar." },
        "hud.movementGrid": { "type": "string", "description": "Persistent HUD tag while movement is grid-aligned (default)." },
        "hud.movementScreen": { "type": "string", "description": "Persistent HUD tag while movement is screen-relative." },
        "hint.fullscreen": { "type": "string", "description": "Hint shown while the game is drawn below 1080 pixels high." },
        "banner.room": { "type": "string", "description": "Banner on entering a room; {room} is the room name." },
        "banner.biome": { "type": "string", "description": "Line under the room banner; {biome} is the biome name." },
        "msg.boot": { "type": "string", "description": "Terminal message when the game starts." },
        "msg.welcome": { "type": "string", "description": "Second terminal message when the game starts." },
        "msg.die": { "type": "string", "description": "Terminal message when the wizard falls into a hole." },
        "msg.void": { "type": "string", "description": "Terminal message when the wizard lands on a void block." },
        "msg.derez": { "type": "string", "description": "Terminal message when the wizard loses his last integrity point." },
        "msg.respawn": { "type": "string", "description": "Terminal message when the wizard recompiles at the room entrance." },
        "msg.plug": { "type": "string", "description": "Terminal message when a pushed object fills a hole." },
        "msg.movementGrid": { "type": "string", "description": "Terminal message when movement switches to grid-aligned." },
        "msg.movementScreen": { "type": "string", "description": "Terminal message when movement switches to screen-relative." }
      }
    }
  }
}
